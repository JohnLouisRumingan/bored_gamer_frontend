import React from 'react'
import { connect } from 'react-redux'


const Login = () => {

    return (
        <div>
            Login Form here
        </div>
    )
}

export default connect(mapStateToProps)(Login)